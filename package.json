{
  "name": "laser-ldr-turbidity-tester",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n vite,api -c blue,green \"vite\" \"npm:dev:api\"",
    "dev:vite": "vite",
    "dev:api": "node --env-file=.env.local server-dev.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "vite": "^6.0.0"
  }
}
